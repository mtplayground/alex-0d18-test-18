export const UPLOAD_FIELD_NAMES = new Set(["files", "images"]);
export const MAX_UPLOAD_FILES = 20;
export const MAX_IMAGE_SIZE_BYTES = 10 * 1024 * 1024;

export const ALLOWED_IMAGE_CONTENT_TYPES = new Set([
  "image/jpeg",
  "image/png",
  "image/webp",
  "image/gif",
]);
